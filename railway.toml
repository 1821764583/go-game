[build]
builder = "nixpacks"
buildCommand = "npm run build --workspace=packages/engine && npm run build --workspace=apps/backend"

[deploy]
startCommand = "node apps/backend/dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3